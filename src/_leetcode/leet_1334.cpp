/*
 * ====================== leet_1334.cpp ==========================
 *                          -- tpr --
 *                                        CREATE -- 2020.08.11
 *                                        MODIFY -- 
 * ----------------------------------------------------------
 * 1334. 阈值距离内邻居最少的城市
 */
#include "innLeet.h"
#include "GraphNode1.h"


namespace leet_1334 {//~


// 计算每个城市，在限定阈值内，可以达到的所有城市数量
// 在数量最少的集合中，挑选 id 最大的那个



class S{
public:
    // n 个城市，{0,...n-1}
    // 2 <= n <= 100
    // 1 <= edges.length <= n * (n - 1) / 2
    // edges[i].length == 3
    // 0 <= fromi < toi < n
    // 1 <= weighti, distanceThreshold <= 10^4
    // 所有 (fromi, toi) 都是不同的
    int findTheCity( int n, std::vector<std::vector<int>>& edges, int distanceThreshold ){




        return 0;
    }
};



//=========================================================//
void main_(){





    

    debug::log( "\n~~~~ leet: 1334 :end ~~~~\n" );
}
}//~
